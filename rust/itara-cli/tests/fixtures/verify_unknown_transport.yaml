nodes:
  - id: gatewayNode
    component: gateway
  - id: workerNode
    component: worker

connections:
  - to: gatewayNode
    transport:
      id: http
      params:
        port: "8080"
  - from: gatewayNode
    to: workerNode
    transport:
      id: grpc
      params:
        port: "8081"
