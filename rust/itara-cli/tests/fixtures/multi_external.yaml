nodes:
  - id: apiNode
    component: api
  - id: workerNode
    component: worker

connections:
  - to: apiNode
    transport:
      id: http
      params:
        port: "8080"
  - to: apiNode
    transport:
      id: http
      params:
        port: "9090"
  - from: apiNode
    to: workerNode
    transport:
      id: http
      params:
        port: "8081"
