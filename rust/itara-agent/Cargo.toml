[package]
name = "itara-agent"
version = "0.1.0"
edition = "2021"

[dependencies]
itara-core           = { path = "../itara-core" }
itara-config         = { path = "../itara-config" }
itara-transport-http = { path = "../itara-transport-http" }

[lib]
crate-type = ["rlib"]
