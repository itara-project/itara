[package]
name = "calculator-api"
version = "0.1.0"
edition = "2021"

[dependencies]
itara-core = { path = "../itara-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
crate-type = ["rlib", "cdylib"]
