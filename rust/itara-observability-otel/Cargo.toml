[package]
name = "itara-observability-otel"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib", "cdylib"]
 
[dependencies]
itara-core  = { path = "../itara-core" }
reqwest     = { version = "0.12", features = ["blocking", "json"] }
serde_json  = "1"
