
[package]
name = "itara-core"
version = "0.1.0"
edition = "2021"

[dependencies]
libloading = "0.8"
serde_json = "1"
uuid       = { version = "1", features = ["v4"] }
base64     = "0.22"

[lib]
crate-type = ["rlib"]
