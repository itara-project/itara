[artifact]
kind    = "api"
id      = "${rootArtifactId}"
version = "${version}"

[runtime]
language = "java"

[itara]
spec-version = "0.2"

# TODO: minimal stub — review these fields, and declare [serializers] and
# non-idempotent [methods] as needed.