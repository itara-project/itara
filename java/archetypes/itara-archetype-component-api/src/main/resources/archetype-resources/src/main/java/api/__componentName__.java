package ${package}.api;

import io.itara.api.ComponentInterface;
import io.itara.api.ContractMethod;

/**
 * TODO: replace exampleMethod with the real contract this component exposes.
 */
@ComponentInterface(id = "${artifactId}")
public interface ${componentName} {

    @ContractMethod
    void exampleMethod();
}