[artifact]
kind        = "component"
id          = "${artifactId}"
version     = "${version}"
api-version = "${apiVersion}"

[runtime]
language = "java"
compiler = "${javaVersion}"

[itara]
spec-version = "0.1"

# TODO: minimal stub — review these fields, and declare [api-dependencies]
# for any other components this one calls.