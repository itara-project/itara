nodes:
  - id: "gatewayNode"
    component: "gateway"

  - id: "calculatorNode"
    component: "calculator"

connections:
  - from: "gatewayNode"
    to: "calculatorNode"
    transport:
      id: http
      params:
        host: "localhost"
        port: 8081
    serializer: "json"