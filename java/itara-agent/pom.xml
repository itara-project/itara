<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>dev.itara</groupId>
        <artifactId>itara-parent</artifactId>
        <version>0.1.0</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>itara-agent</artifactId>
    <name>Itara Java Agent</name>

    <dependencies>
        <!-- itara-core must be on the classpath at agent startup.
             It is shaded into the fat jar so the agent is fully
             self-contained and does not depend on the application
             classloader having it available. -->
        <dependency>
            <groupId>dev.itara</groupId>
            <artifactId>itara-core</artifactId>
        </dependency>
        <!-- Jackson YAML for wiring config parsing.
                 Shaded into the fat jar to avoid version conflicts with any
                 YAML or Jackson library the application may use.
                 Provides ObjectMapper-style deserialization directly into
                 WiringConfig, ComponentEntry, and ConnectionEntry. -->
        <dependency>
            <groupId>com.fasterxml.jackson.dataformat</groupId>
            <artifactId>jackson-dataformat-yaml</artifactId>
        </dependency>
        <!-- Jackson TOML for `.itara` metadata file parsing.
             Shaded into the fat jar for the same reason as the YAML
             dataformat above. Provides ObjectMapper-style deserialization
             directly into MetadataFile / ArtifactMeta / etc. -->
        <dependency>
            <groupId>com.fasterxml.jackson.dataformat</groupId>
            <artifactId>jackson-dataformat-toml</artifactId>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- These five are declared here, not in itara-parent, so only itara-agent
                 (and itara-core, which declares the same set) runs them. Every other
                 module in the reactor is unaffected. Config/versions come from
                 itara-parent's pluginManagement. -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>flatten-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.sonatype.central</groupId>
                <artifactId>central-publishing-maven-plugin</artifactId>
            </plugin>

            <!--
                Shade plugin produces a fat jar containing itara-core.
                This ensures the agent is self-contained:
                it must be loadable before the application classloader
                is initialized, so it cannot rely on application classpaths.

                The MANIFEST declares Premain-Class so the JVM knows
                which class to call before main().
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <shadedArtifactAttached>false</shadedArtifactAttached>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <filters>
                                <!-- Exclude signature files that break fat jars -->
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                            <transformers>
                                <transformer
                                        implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <manifestEntries>
                                        <Premain-Class>dev.itara.agent.ItaraAgent</Premain-Class>
                                        <Can-Redefine-Classes>true</Can-Redefine-Classes>
                                        <Can-Retransform-Classes>true</Can-Retransform-Classes>
                                    </manifestEntries>
                                </transformer>
                            </transformers>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <!-- GPG signing lives here, not in the plugins list above, so an
         ordinary `mvn install` never touches GPG at all — no key needed,
         no passphrase prompt. Only `mvn <goal> -Prelease` signs anything. -->
    <profiles>
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>