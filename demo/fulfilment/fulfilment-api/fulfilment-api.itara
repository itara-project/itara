[artifact]
kind = "api"
id = "fulfilment"
version = "0.1.0"
api-version = "0.x"

[runtime]
language = "java"
compiler = "21"

[itara]
spec-version = "0.1"
core-version = "0.1+"

[methods]
non-idempotent = ["processOrder"]
# cancelOrder is idempotent — cancelling an already-cancelled order is safe
