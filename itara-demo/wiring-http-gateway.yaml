# wiring-http-gateway.yaml
# Gateway JVM: calls Calculator over HTTP on localhost:8081

components:
  - id: "gateway"

connections:
  - from: "gateway"
    to: "calculator"
    type: http
    host: "localhost"
    port: 8081
    serializer: "json"
